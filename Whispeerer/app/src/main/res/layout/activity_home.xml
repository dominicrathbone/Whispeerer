<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="fill_parent"
    android:layout_height="fill_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".HomeActivity">


    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:text="@string/welcome_text"
        android:id="@+id/welcomeText"
        android:layout_marginTop="118dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/start_chat_button"
        android:id="@+id/startChat"
        android:layout_below="@+id/welcomeText"
        android:layout_marginTop="31dp"
        android:layout_alignRight="@+id/welcomeText"
        android:layout_alignEnd="@+id/welcomeText"
        android:layout_alignLeft="@+id/welcomeText"
        android:layout_alignStart="@+id/welcomeText"
        android:onClick="openStartChatActivity"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/chat_requests_button"
        android:id="@+id/chatRequests"
        android:layout_below="@+id/startChat"
        android:layout_alignLeft="@+id/startChat"
        android:layout_alignStart="@+id/startChat"
        android:layout_alignRight="@+id/startChat"
        android:layout_alignEnd="@+id/startChat"
        android:onClick="openChatRequestsActivity"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/share_button"
        android:id="@+id/shareButton"
        android:layout_below="@+id/chatRequests"
        android:layout_alignLeft="@+id/chatRequests"
        android:layout_alignStart="@+id/chatRequests"
        android:layout_alignRight="@+id/chatRequests"
        android:layout_alignEnd="@+id/chatRequests"
        android:onClick="openShareIntent" />

</RelativeLayout>
